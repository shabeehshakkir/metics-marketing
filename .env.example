# SMTP Configuration for Contact Form
# By default, emails are routed to the local Mailpit container for testing.
# To send real emails, comment out the Mailpit defaults and provide your SMTP details below:

# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASSWORD=your_api_key_here
# SMTP_FROM=noreply@yourdomain.com
# SMTP_AUTH=on
# SMTP_TLS=on
# SMTP_STARTTLS=on
